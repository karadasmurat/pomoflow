---
name: commit-push
description: Stages all changes, writes a conventional commit message, and pushes to current branch.
---

Review all staged and unstaged changes using `git diff`. Write a concise commit message following conventional commits format (feat/fix/chore/docs etc.). Stage all changes with `git add -a`, commit, and push to the current branch.
